package net.bancer.sparkdict.domain.parsers;

/**
 * All data types that can be present in the <dictionary name>.dict file.
 * 
 * @author Valerij Bancer
 *
 */
public enum DataType {
	m, l, g, t, x, y, k, w, h, r, W, P, X, n
}
